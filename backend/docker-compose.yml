version: "3"
services:
    db:
        image: postgres:latest
        ports:
            - "5432:5432"
        environment:

    api:
        build: .
        ports:
            - "8080:8080"
        environment:
        depends_on:
            - db
